void printSubstrings(char input[]) {
    int k = 0;
    for(int i = 0; input[i] != '\0'; i++) {
			k = i;
        while (k < strlen(input)){
            for(int j = i; j <= k; j++) {
			cout << input[j];
            }
            cout<<endl;
            k++;
        
        }
    }
}
